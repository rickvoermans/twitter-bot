import config

# a .gitignore should include a reference to the config file
api_key = config.api_key
api_secret = config.api_secret

oauth_token = config.oauth_token
oauth_secret = config.oauth_secret
